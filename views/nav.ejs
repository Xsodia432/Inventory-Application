<nav>
  <div class="nav-content">
    <a href="/"><h1>Home</h1></a>
    <% if(id !== "null") {%> <% if(currentPath === "/") {%><button
      class="modal-show"
    >
      Add Category
    </button>
    <%}%> <% if(currentPath.startsWith("/category")) {%><button
      class="modal-show-items"
      onclick="addItem('<%=id%>')"
    >
      Add Item
    </button>
    <%}%> <%}%>
  </div>
</nav>
